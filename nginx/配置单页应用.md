# nginx config for spa

```ini
location /baseUrl/ {
    # 代码文件，nginx根路径的相对路径
    alias target-folder/;
    index index.html;
    # 将404导向baseUrl
    error_page 404 = /baseUrl/;
}
```

- 访问的URL最后必须带`/`