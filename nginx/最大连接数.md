# nginx 最大连接数

- `worker_processes` 进程数 

- `worker_connections` 每个进程的连接数

- 最大连接数 = `worker_processes` * `worker_connections`

> 注意：
> 1. 最大连接数还受`worker_rlimit_nofile`影响，
此参数为进程最大打开文件数，受限于系统
> 2. 最大连接数不仅包含与客户端的连接，还包含与被代理服务器的连接